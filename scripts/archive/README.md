# Archived Scripts

Scripts under this directory are retained for migration/history purposes and are not part of the current developer workflow.

Active bootstrap, validation, benchmark, ORION, and quality utilities live directly under `scripts/` or a maintained subdirectory such as `scripts/orion/`.

Do not update an archived migration script to support a new architecture. Replace the workflow with a current tool or a one-time migration issue/PR instead.
